'use client';

// Este componente ya no es necesario porque la lista está en la página principal
// Lo dejo como placeholder por si se necesita en el futuro

export default function FlowList() {
  return null;
}
